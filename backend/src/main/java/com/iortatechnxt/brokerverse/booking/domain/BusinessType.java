package com.iortatechnxt.brokerverse.booking.domain;

/** New Business vs Renewal classification of a booked invoice (BRNB.097). */
public enum BusinessType {
  /** New Business (BRD-1). */
  NEW_BUSINESS,
  /** Renewal (later BRD). */
  RENEWAL
}
