package com.iortatechnxt.brokerverse.catalog.domain;

/** Level of a sales organisation unit (BRNB.075). */
public enum SalesLevel {
  /** Region. */
  REGION,
  /** Department within a region. */
  DEPARTMENT,
  /** Team within a department; account officers belong to teams. */
  TEAM
}
