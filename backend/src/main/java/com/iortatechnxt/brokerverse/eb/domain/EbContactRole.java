package com.iortatechnxt.brokerverse.eb.domain;

/** Role of a client contact of a programme (design 4.2). */
public enum EbContactRole {
  /** Head of human resources. */
  HR_HEAD,
  /** HR officer. */
  HR_OFFICER,
  /** Finance (SOA, billing). */
  FINANCE
}
