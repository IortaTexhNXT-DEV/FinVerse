package com.iortatechnxt.brokerverse.issuance.domain;

/** Business action started by an uploaded document (BRNB.105). */
public enum TriggerAction {
  /** Extract the policy data and open an extraction review task. */
  EXTRACTION_REVIEW
}
