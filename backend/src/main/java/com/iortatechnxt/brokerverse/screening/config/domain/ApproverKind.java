package com.iortatechnxt.brokerverse.screening.config.domain;

/**
 * How an approval route names its approver (SNSRP-103): the unit head of the client's marketing
 * unit, a role or a user.
 */
public enum ApproverKind {
  UNIT_HEAD,
  ROLE,
  USER
}
