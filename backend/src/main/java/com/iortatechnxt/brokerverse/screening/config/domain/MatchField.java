package com.iortatechnxt.brokerverse.screening.config.domain;

/** Field compared by a matching rule (SNSRP-101); NAME is always compared. */
public enum MatchField {
  NAME,
  ALIAS,
  BIRTH_DATE,
  NATIONALITY,
  ID
}
