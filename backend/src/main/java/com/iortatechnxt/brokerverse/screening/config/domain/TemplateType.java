package com.iortatechnxt.brokerverse.screening.config.domain;

/**
 * Type of a review or STR template (SNSRP-104, 105); the scope of a TEMPLATE configuration version.
 */
public enum TemplateType {
  KYC_REVIEW,
  TRANSACTION_REVIEW,
  EDD,
  STR
}
