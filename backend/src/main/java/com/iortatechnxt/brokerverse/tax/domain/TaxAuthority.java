package com.iortatechnxt.brokerverse.tax.domain;

/** Authority a return is filed with. */
public enum TaxAuthority {
  /** Bureau of Internal Revenue (national taxes). */
  BIR,
  /** Local government unit (local business tax on premiums). */
  LGU,
  /** Bureau of Fire Protection (fire service tax). */
  BFP
}
