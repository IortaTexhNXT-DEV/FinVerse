package com.iortatechnxt.finverse.claims.domain;

/** Motor cover under which a repair is ordered. */
public enum LpoCover {
  /** Own damage of the insured vehicle. */
  OD,
  /** Third-party vehicle damaged by the insured. */
  TP
}
