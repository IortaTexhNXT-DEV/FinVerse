# Start-up safeguards of a production deployment (seed data guard, required secrets).
org.springframework.boot.env.EnvironmentPostProcessor=\
  com.iortatechnxt.brokerverse.config.ProductionSafeguards
